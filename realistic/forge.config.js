/*
The actual Electron Forge configuration for this project is a responsibility of the "build-support" library. We just
need to re-export the configuration from here so that the Electron Forge CLI can find it.
*/
module.exports = require("electron-playground_realistic_build-support/config");
